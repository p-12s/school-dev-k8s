---
# file: practice/2.application-abstractions/1.pod/pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-pod # два пода с таким именем существовать не могут
spec:
  containers:
  - image: quay.io/testing-farm/nginx:1.12
    name: nginx
    ports:
    - containerPort: 80
...
